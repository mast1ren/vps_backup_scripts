[Unit]
Description=VPS Backup Monitor Service
After=network.target

[Service]
Type=simple
# 修改为脚本实际安装目录后再启用服务
Environment=VPS_BACKUP_SCRIPTS_DIR=/opt/vps_backup_scripts
ExecStart=/bin/bash ${VPS_BACKUP_SCRIPTS_DIR}/monitor.sh
Restart=always
User=root

[Install]
WantedBy=multi-user.target
