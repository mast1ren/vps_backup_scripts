[Unit]
Description=VPS Backup Script (Daily 2:00)
After=network.target

[Service]
Type=oneshot
# 修改为 backup.sh 的实际安装路径后再启用服务
Environment=BACKUP_SCRIPT=/opt/vps_backup_scripts/backup.sh
ExecStart=/bin/bash ${BACKUP_SCRIPT}
User=root

[Install]
WantedBy=multi-user.target
